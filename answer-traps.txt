问题一:哪些寄存器用于保存函数的参数呢？例如，在 main 函数调用 printf 时，哪个寄存器中保存着数值 13 呢？

根据 call.asm 的汇编，a2 保存着数值13

问题二:在 main 函数的汇编代码中， f 函数的调用位置在哪里？ g 函数的调用位置又在哪里呢？（提示：编译器可能会将某些函数内联处理）

f 和 g 均没有被调用，编译器直接计算出了结果，没有产生调用

问题三:printf 函数位于哪个地址呢？

位于 0x628

问题四:在 main 中，位于 jalr 到 printf 之后的 ra 寄存器里的值是多少呢？

0x38

问题五:

运行以下代码。

	unsigned int i = 0x00646c72;
	printf("H%x Wo%s", 57616, &i);
      
unsigned int i = 0x00646c72;
printf("H%x Wo%s", 57616, &i);

输出结果是什么？以下是一个将字节转换为字符的 ASCII 表。

输出结果取决于 RISC-V 采用的是小端字节序这一事实。如果 RISC-V 采用大端字节序，那么为了得到相同的输出结果，你应该将 i 设置为多少呢？是否还需要将 57616 的值也更改一下呢？

以下是对小端序和大端序的简单说明，另外还有一种比较有趣的描述方式。


输出结果: He110 World

问题6:

在下面的代码中， 'y=' 之后会打印出什么内容呢？（注意：答案并非某个具体的数值。）为什么会这样呢？

	printf("x=%d y=%d", 3);

y 的值是不确定的，属于未定义行为